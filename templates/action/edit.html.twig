{% extends 'base.html.twig' %}

{% block title %}Edit Action{% endblock %}

{% block body %}
    {% set user = app.user %}
    {% include('app/dashboard/_dashboardHeader.html.twig') %}
    {% include('app/dashboard/_dashboardAside.html.twig') %}

    <main id="main" class="main">
        <section>
            <div class="row">
                <div class="col-12">
                    {{ include('action/_formUserAction.html.twig', {'button_label': 'Update'}) }}
                </div>
                <div class="d-flex justify-content-between mt-2">
                        <a class="btn btn-sm btn-link" href="{{ path('dashboard_index', {'nni': app.user.nni}) }}">
                            <i class="bi bi-arrow-left"></i>
                            Retourner sur le tableau de bord
                        </a>
                     {{ include('action/_delete_form.html.twig') }}
                </div>
            </div>

        </section>
    </main>


{% endblock %}
